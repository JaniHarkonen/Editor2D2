TODO - MajorBugfixes

OVERVIEW
- make sure that all required functionalities have been
  implemented and generally work as intended

SPECIFICS
- test project Project tab
	-- test loading a project
	-- test saving a project
	-- test saving project as
- test AssetPane
	-- test creating assets / folders
	-- test multiple assets
	-- test renaming assets / folders
	-- test editing assets / folders
	-- test deleting assets / folders
- test ModalWindow
	-- test asset creation
	-- test multiple assets
	-- check that modals have all required fields
	   and that fields cannot be broken
- test PropertiesPanes
	-- check all requried fields are displayed
	-- check all required fields have validation
	-- test placed Placeable vs non-placed
- test SceneControlsPane
	-- test resizing
	-- test grid settings
	-- test grid toggles
- test SceneView
	-- test tab switching
	-- test tab creation
	-- test tab deletion
	-- test tab renaming
	-- test tab meta data settings
		o test setting compilation statement
- test Tools and Toolbar
	-- test Tool switching
	-- test Tool hotkeys
	-- test Tool tooltip
	-- display Tool description
	-- test selection, additive, subtractive (undo, redo)
	-- test move (undo, redo)
	-- test place selection, deletion, allow stack, single (undo, redo)
	-- test flood fill (undo, redo)
	-- test rotate (undo, redo)
	-- test scale, vertical, horizontal (undo, redo)
- test LayerManagerPane
	-- test layer selection
	-- test layer visibility toggle
	-- test layer addition
	-- test layer deletion
	-- test layer editing
		o test back
		o test name, validation
		o test setting grid dimensions
		o test setting opacity
		o test setting visibility


REMEMBER: 	Instance layer selection bug is due to
			an off by one error in the
			selectPlaceables(int,int,int,int).
			
REMEMBER2:
			HotkeyListener becomes messed up when the
			window looses focus or a popup is displayed
			while keys are being held.

LATER
- Layer writer should write whether the Layer is visible
- make sure that GUIComponents dont update on HotkeyListner
  if no expected sequence is held
- make sure that GUIComponent.update is called only upon
  onNotification as often as possible to avoid littering
  the GUI-code with "update()"
- ? should SelectionManagers be declared in the AppState
- de-messify Root
- speedup Tile rendering performance by getting the subimage
  only when the Image the Tile is based on changes
	-- check other Placeables for render optimizations
	-- pay attention to memory usage as well
- fix object selection and selection rendering
	   
TASKS LEFT
- create abstractions for compiling
	-- compiler is to be implemented on project-by-project
	   basis, and thus, only abstract classes and interfaces
	   need to be written
- make sure that all the functionalities work with minor
  bugs
- finalize graphics
- remove DebugUtils and other debug prints and resources
	-- JohnnyUtils
- fix remaining minor bugs
- create a TEMPLATE module
- remove comments, add Javadoc
	   
	   
SKETCHES

---- Editor2D savefile ----

assets
	data "collision data" DATA00001 "0" 255 0 0
	image "unknown image" IMG555WETIP "D:\epic\assoomer_logo.png"
	image "coomer" IMG555WECOOM "D:\epic\coomer_profile.png"
	object "cj_coomer" OBJ555WECOOM 128 64 0
/assets

scene "first scene" 640 480
	layer data "data layer" 32 32 128
		DATA00001 5 5
		DATA00001 5 6
		DATA00001 5 7
		DATA00001 5 8
	/layer
	
	layer tile "Max Speed" 32 32 255
		IMG555WETIP 0 0
		IMG555WETIP 1 0
		IMG555WETIP 2 0
		IMG555WETIP 3 0
		IMG555WETIP 4 0
	/layer
	
	layer object "'bjects" 0 0 255
		OBJ555WECOOM 36 36
	/layer
/scene
