TODO - ArchitecturalRethink

- critically examine the current architecture
- make sure that the current architecture is ready for the next
  software components

- find redundancies in code
- figure out the correct usage of SubscriberService
- determine if some listeners can be turned into GUIComponents
- determine a way to generate menu buttons (e.g. when creating an asset)
  in a general fashion to cut out weird dependencies to GUI components
- determine if container classes are needed in Root
- figure out if some GUI classes can be split further
- figure out if some GUI classes can be combined
- figure out the role of the Controller as well as AppState